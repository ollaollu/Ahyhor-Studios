        	<div class="master">
        		<div id="imgprop" width="238" height="238">
             	<%= image_tag(item.image.url, {:id => dom_id(item), :title => item.title, :width => "238", :height => "238"}) %>
             	</div>

	             <div id="ps1" class="popit">
	             	<section>
	             		  <% if ::Refinery::Portfolio.display_item_text %>
						      <span><%= item.title %></span>
						      <span><%= raw item.caption %></span>
						      <span id="zoom"></span>
						  <% end %>       		
	             	</section>
	             </div>             	
            </div>

       		<div id="GbgPopup1">
				<div id="GPopup1">
					<div class="close1"></div>
					<span class="ecs_tooltip"><span class="arrow"></span></span>
					<div class="image1">
					<%= image_tag(item.image.url, {:id => dom_id(item), :title => item.title, :width => "810", :height => "470"}) %> 
					</div>      
				</div>
			</div>
  